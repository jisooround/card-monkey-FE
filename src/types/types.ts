export interface BenefitProps {
  item: string;
}

export interface signType {
  userId: string;
  password: string;
  name: string;
}

export interface loginType {
  id: string;
  password: string;
}
